<p style="color: green"><%= notice %></p>

<h1>Forms</h1>

<div id="forms">
  <% @forms.each do |form| %>
    <%= form.name %>
    <p>
      <%= link_to "Show this form",form_path(form, userid: @user.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New form", new_form_path(userid: @user.id) %>
