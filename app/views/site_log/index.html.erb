<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comprehensive Log Viewer</title>
    <style>
        body {
            font-family: 'Courier New', monospace;
            line-height: 1.6;
            color: #e0e0e0;
            background-color: #1e1e1e;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background-color: #2d2d2d;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.5);
        }
        h1 {
            color: #ffffff;
            border-bottom: 2px solid #4a4a4a;
            padding-bottom: 10px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #b0b0b0;
        }
        input[type="date"], input[type="text"] {
            width: 100%;
            padding: 8px;
            background-color: #3a3a3a;
            border: 1px solid #555;
            border-radius: 4px;
            color: #e0e0e0;
        }
        button {
            background-color: #4a4a4a;
            color: #ffffff;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #5a5a5a;
        }
        .logs {
            margin-top: 20px;
            border: 1px solid #4a4a4a;
            border-radius: 4px;
            overflow: hidden;
        }
        .log-header {
            background-color: #3a3a3a;
            padding: 10px;
            border-bottom: 1px solid #4a4a4a;
            font-weight: bold;
            color: #ffffff;
        }
        .log-container {
            max-height: 700px;
            overflow-y: auto;
        }
        .log-entry {
            padding: 10px;
            border-bottom: 1px solid #3a3a3a;
            cursor: pointer;
        }
        .log-entry:hover {
            background-color: #3a3a3a;
        }
        .log-entry:nth-child(even) {
            background-color: #2a2a2a;
        }
        .log-entry pre {
            margin: 0;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
        .no-logs {
            padding: 20px;
            text-align: center;
            color: #888;
        }
        .log-level {
            display: inline-block;
            padding: 2px 5px;
            border-radius: 3px;
            margin-right: 5px;
            font-weight: bold;
        }
        .log-level-info { background-color: #2c5282; color: #ffffff; }
        .log-level-warn { background-color: #92400e; color: #ffffff; }
        .log-level-error { background-color: #9b2c2c; color: #ffffff; }
        .log-level-debug { background-color: #2f855a; color: #ffffff; }
        .log-level-fatal { background-color: #742a2a; color: #ffffff; }
        .log-timestamp {
            color: #888;
            margin-right: 10px;
        }
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.4);
        }
        .modal-content {
            background-color: #2d2d2d;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #4a4a4a;
            width: 80%;
            max-width: 800px;
        }
        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }
        .close:hover,
        .close:focus {
            color: #fff;
            text-decoration: none;
            cursor: pointer;
        }
        .log-entry pre {
            margin: 0;
            white-space: pre-wrap;
            word-wrap: break-word;
            max-height: 100px;
            overflow: hidden;
        }
        .modal-content pre {
            white-space: pre-wrap;
            word-wrap: break-word;
            max-height: 80vh;
            overflow-y: auto;
        }
        .form-group {
            display: inline-block;
            margin-right: 15px;
        }
        select {
            padding: 8px;
            background-color: #3a3a3a;
            border: 1px solid #555;
            border-radius: 4px;
            color: #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Comprehensive Log Viewer</h1>

        <form action="/site_log" method="get">
            <div class="form-group">
                <label for="date">Date:</label>
                <input type="date" id="date" name="date" value="<%= @date %>">
            </div>
            <div class="form-group">
                <label for="time_filter">Time Range:</label>
                <select id="time_filter" name="time_filter">
                    <option value="1_hour" <%= 'selected' if @time_filter == '1_hour' %>>Last 1 hour</option>
                    <option value="6_hours" <%= 'selected' if @time_filter == '6_hours' %>>Last 6 hours</option>
                    <option value="12_hours" <%= 'selected' if @time_filter == '12_hours' %>>Last 12 hours</option>
                    <option value="24_hours" <%= 'selected' if @time_filter == '24_hours' %>>Last 24 hours</option>
                    <option value="all_day" <%= 'selected' if @time_filter == 'all_day' %>>All day</option>
                </select>
            </div>
            <div class="form-group">
                <label for="length_filter">Number of Entries:</label>
                <select id="length_filter" name="length_filter">
                    <option value="5" <%= 'selected' if @length_filter == '5' %>>5</option>
                    <option value="10" <%= 'selected' if @length_filter == '10' %>>10</option>
                    <option value="25" <%= 'selected' if @length_filter == '25' %>>25</option>
                    <option value="50" <%= 'selected' if @length_filter == '50' %>>50</option>
                    <option value="100" <%= 'selected' if @length_filter == '100' %>>100</option>
                </select>
            </div>
            <div class="form-group">
                <label for="query">Search:</label>
                <input type="text" id="query" name="query" value="<%= @query %>" placeholder="Search logs...">
            </div>
            <button type="submit">Filter</button>
        </form>

        <div class="logs">
            <div class="log-header">
                Showing <%= @logs.length %> most recent logs for <%= @date %> (<%= @time_filter.gsub('_', ' ') %>)
            </div>
            <% if @logs.any? %>
                <div class="log-container">
                    <% @logs.each_with_index do |log, index| %>
                        <div class="log-entry" onclick="showModal('<%= index %>')">
                            <%
                                first_line = log.split("\n").first
                                parts = first_line.split(' ', 4)
                                timestamp = parts[0] + ' ' + parts[1]
                                level = parts[2].downcase
                                message = parts[3] || ''
                            %>
                            <span class="log-timestamp"><%= timestamp %></span>
                            <span class="log-level log-level-<%= level %>"><%= level.upcase %></span>
                            <pre><%= message %></pre>
                        </div>
                    <% end %>
                </div>
            <% else %>
                <div class="no-logs">
                    No logs found for the selected criteria.
                </div>
            <% end %>
        </div>
    </div>

    <div id="logModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal()">&times;</span>
            <pre id="modalContent"></pre>
        </div>
    </div>

    <script>
        var modal = document.getElementById("logModal");
        var modalContent = document.getElementById("modalContent");
        var logs = <%= raw @logs.to_json %>;

        function showModal(index) {
            modalContent.textContent = logs[index];
            modal.style.display = "block";
        }

        function closeModal() {
            modal.style.display = "none";
        }

        window.onclick = function(event) {
            if (event.target == modal) {
                closeModal();
            }
        }
    </script>
</body>
</html>