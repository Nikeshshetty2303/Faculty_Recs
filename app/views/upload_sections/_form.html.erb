<%= form_with(model: upload_section) do |form| %>
  <% if upload_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload_section.errors.count, "error") %> prohibited this upload_section from being saved:</h2>

      <ul>
        <% upload_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
