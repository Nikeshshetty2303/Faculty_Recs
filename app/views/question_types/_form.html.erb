<%= form_with(model: question_type) do |form| %>
  <% if question_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(question_type.errors.count, "error") %> prohibited this question_type from being saved:</h2>

      <ul>
        <% question_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
