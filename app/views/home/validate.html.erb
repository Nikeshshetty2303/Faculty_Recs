<%= render partial: 'partials/navbar', locals: { user: @user, locale: true } %>

<div class="container">
  <%= link_to "Go Back", :back, class: "btn btn-light mt-3" %>
  <table class="table table-bordered table-striped">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Application ID</th>
        <th scope="col">Credit Score</th>
        <th scope="col">Validate Credits</th>
        <th scope="col">Verified Credits</th>
        <th scope="col">Academic Experience</th>
        <th scope="col">Professional Experience</th>
        <th scope="col">Credit Requirements</th>
        <th scope="col">Eligibility</th>
      </tr>
    </thead>
    <tbody>
      <% responses = Response.all
           .select { |response| response.form.present? && response.form.department.title == @user.validator && response.status == "Freezed" }

         sorted_responses = responses.sort_by do |response|
           if response.app_no.present?
             match = response.app_no.match(/^AP(\d+)(\w{2})(\d+)$/)
             if match
               [match[1].to_i, match[2], match[3].to_i]
             else
               [Float::INFINITY, '', Float::INFINITY]
             end
           else
             [Float::INFINITY, '', Float::INFINITY]
           end
         end

         sorted_responses.each do |response| %>
        <tr>
          <td><%= response.app_no if response.app_no.present? %></td>
          <td><%= response.credit_score %></td>
          <td><%= link_to "View Response", validate_response_path(id: response.id), class: 'btn btn-outline-primary btn-sm' %></td>
          <td><%= response.validated_credit_score %></td>
          <td>
            <%= response.academic_experience ? "Satisfactory" : "Not Satisfactory" %>
          </td>
          <td>
            <%= response.professional_experience ? "Satisfactory" : "Not Satisfactory" %>
          </td>
          <td>
            <%= response.credit_requirements ? "Satisfactory" : "Not Satisfactory" %>
          </td>
          <td>
            <%= response.eligibility ? "Eligible" : "Not Eligible" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>